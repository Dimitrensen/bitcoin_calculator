apiVersion: v1
kind: Service
metadata:
  name: {{ include "btc-microservice.fullname" . }}
  labels:
    {{- include "btc-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "btc-microservice.selectorLabels" . | nindent 4 }}
